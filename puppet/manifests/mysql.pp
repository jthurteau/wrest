# --not needed
# package { "mysql":
#   ensure  => present,
#   require => Exec["yum update -y"],
# }
# --not needed
# package { "mysql-server":
#   ensure  => present,
#   require => Package["postgresql"],
# }
